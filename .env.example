# 環境変数設定の例
# 実際に使用する場合は .env ファイルを作成してください（.gitignoreで除外済み）

# アプリケーション設定
ENVIRONMENT=development
PORT=8080

# GCP設定（ローカル開発用）
GOOGLE_CLOUD_PROJECT=your-project-id
GOOGLE_APPLICATION_CREDENTIALS=path/to/your/credentials.json
