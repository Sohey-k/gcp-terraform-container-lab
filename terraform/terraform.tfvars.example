# このファイルをコピーして terraform.tfvars を作成してください
# cp terraform.tfvars.example terraform.tfvars

# GCP Project ID (必須)
project_id = "your-gcp-project-id"

# Docker イメージのパス (必須)
docker_image = "docker.io/your-dockerhub-username/gcp-free-app:latest"

# 環境名 (dev, staging, prod のいずれか)
environment = "dev"

# リージョンとゾーン (無料枠を使う場合は us-central1 を推奨)
region = "us-central1"
zone   = "us-central1-a"

# マシンタイプ (無料枠は e2-micro)
machine_type = "e2-micro"

# SSH接続を許可するソースIP範囲 (例: ["YOUR_IP/32"])
# 空の場合、SSHファイアウォールルールは作成されますがアクセスできません
ssh_source_ranges = []
